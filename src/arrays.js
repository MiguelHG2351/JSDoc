//@ts-check

/**
 * Edades
 * @type {array}
 */

let age = [25, 21, 12]

/**
 * Arreglos de Números
 * @type {Array<Number | object>}
 */

let scores = [25, 21, 12, {}, [], true]

/**
 * Arreglo de string's
 * @type {Array<string>}
 */

 const names = ["Miguel", "Angel"]