//@ts-check

/**
 * Function add two number
 * @param {Number} n1 first number
 * @param {Number} n2 second number
 * @returns {Number}
 */

function add(n1, n2) {
    return n1 + n2
}


/**
 * Function add two number
 * @param {Number} n1 first number
 * @param {Number} n2 second number
 * @returns {String}
 */

const addTwo = (n1, n2) => {
    return `${n1 + n2}`
}
